# Schemas Definitions

This directory contains the master JSON schema files (`*.schema.json`).

These exact raw JSON specifications dictate the structures of Subagent Outputs, API payloads, and SQLite storage projections. Pydantic models in `app/models.py` must maintain a 1:1 parity with these files.
